version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm ci
    build:
      commands:
        - npm run build -- --configuration production
  artifacts:
    baseDirectory: dist/arcar/browser
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
